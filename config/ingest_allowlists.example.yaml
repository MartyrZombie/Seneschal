# Copy to config/ingest_allowlists.yaml and customize.
# Admin (Samson) is the only writer; agent user has no path here.

principal_id: samson

# Desktop shim API keys map to principal_id
api_keys:
  dev-local-key: samson

email_senders:
  - samson@example.com

sms_numbers:
  - "+15555550100"

voice_callers:
  - "+15555550100"

# Optional shared-secret prefix for high-consequence SMS commands
# sms_command_prefix: "CMD:"
