# Copy to config/ingest_allowlists.yaml and customize.
# Admin (owner) is the only writer; agent user has no path here.

principal_id: owner

# Desktop shim API keys map to principal_id
api_keys:
  dev-local-key: owner

email_senders:
  - owner@example.com

sms_numbers:
  - "+15555550100"

voice_callers:
  - "+15555550100"

# Optional shared-secret prefix for high-consequence SMS commands
# sms_command_prefix: "CMD:"
